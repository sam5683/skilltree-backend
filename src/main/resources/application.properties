# MongoDB Atlas connection
spring.data.mongodb.uri=mongodb+srv://sameerkaushal35:sameerkaushal@cluster0.ll7htnu.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=skilltree

# Port config (optional)
server.port=8080

# Logging (optional, for debugging MongoDB)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Test workaround (optional, can be removed after test fix)
#spring.classformat.ignore=true